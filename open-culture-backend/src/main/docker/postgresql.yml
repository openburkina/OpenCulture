version: '2'
services:
  openculture-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/openculture/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=openculture
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
